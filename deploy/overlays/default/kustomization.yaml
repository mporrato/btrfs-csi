---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# NOTE: Apply deploy/overlays/snapshot/ first to install VolumeSnapshot CRDs
# and the snapshot-controller if snapshot support is needed.
resources:
  - ../../base
