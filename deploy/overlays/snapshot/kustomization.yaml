---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Installs VolumeSnapshot CRDs, snapshot-controller, and its RBAC.
# Apply this overlay before the driver if snapshot support is needed.
components:
  - ../../components/snapshotter
