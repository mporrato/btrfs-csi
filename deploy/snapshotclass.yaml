---
apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshotClass
metadata:
  name: btrfs-snapshot
driver: btrfs.csi.local
deletionPolicy: Delete
parameters: {}
  # pool: default   # Optional: route snapshots to a named pool.
  #                 # Omit to use the "default" pool (or the sole configured pool).
